#------ 시스템시간 설정
$ sudo date -s '2026-05-01 23:25:14'

#------ 시스템시간을 하드웨시계에 쓰기
$ sudo hwclock -w  --> 과거 명령어
또는
$ sudo hwclock -systohc  --> systemd 환경에서 적용되는 명령어


#------ 하드웨시간을 시스템시간에 반영
$ sudo hwclock -s  --> 과거 명령어
또는
$ sudo hwclock -hctosys  --> systemd 환경에서 적용되는 명령어

